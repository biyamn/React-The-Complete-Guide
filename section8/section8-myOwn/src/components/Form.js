
const Form = () => {
  return (
    <form>
      <label>Username</label>
      <input type='text' />
      <label>Age(Years)</label>
      <input type='text' />
      <button type='submit'>Add User</button>
    </form>
  );
};

export default Form;